

const EditPost = () => {
    return (
        <div>
            {/* {userInfo && userInfo.id === postInfo.author._id && <Button><FaShare className='mx-2' />Edit</Button>} */}

            {/* {userInfo && typeof userInfo === 'object' && userInfo.id &&
 postInfo && typeof postInfo === 'object' && postInfo.author && postInfo.author._id &&
 userInfo.id === postInfo.author._id && (
    <Button className='mx-2'>Edit</Button>
)} */}


            {/* {userInfo && userInfo.id && postInfo && postInfo._id && userInfo.id === postInfo._id && (
    <Button className='mx-2'>Edit</Button>
)} */}


            {/* {userInfo && postInfo && userInfo.id === postInfo.author._id && (
    <>
        <Button onClick={handleDelete}><FaTrash className='mx-2' />Delete</Button>
        <Button><FaShare className='mx-2' />Edit</Button>
    </>
)} */}

        </div>
    )
}

export default EditPost
